#acou3d

This is (currently one but will be several) tests of acoustic modeling in 3D.

* `python-pyvista` attempts to model a speaker enclosure and radiating acoustic field, with acoustic interaction between the driver, cabinet and surrounding air (linear BEM).

* `python-taichi` is a physics-based air-particle simulator, running on GPU if available.
